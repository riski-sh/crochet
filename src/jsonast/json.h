/*
 * The goal of this JSON library is to parse json data structures as if we
 * were parsing the AST of a language. This will remove ambiguity and make
 * sure the user of this api understands the data that they expect to be
 * getting.
 */
#ifndef JSON_H
#define JSON_H

  void

#endif
